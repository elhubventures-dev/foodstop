{
  "name": "chopfast-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@chopfast/web",
    "dev:api": "npm run start:dev --workspace=@chopfast/api",
    "build": "npm run build --workspace=@chopfast/web",
    "start": "npm run start --workspace=@chopfast/web",
    "lint": "npm run lint --workspace=@chopfast/web"
  },
  "dependencies": {
    "motion-utils": "^12.36.0",
    "zustand": "^5.0.12"
  }
}
