# Execution

Deterministic Python scripts live here.

Each script should:
- Handle one specific task (API call, data processing, file operation, etc.)
- Read configuration from `.env` via `python-dotenv`
- Be well-commented and independently testable
- Accept inputs via CLI arguments or stdin
- Return structured output (JSON preferred)
