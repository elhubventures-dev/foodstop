export const metadata = {
  title: 'Vendor registration | FOOD STOP',
  description: 'Register your restaurant as a FOOD STOP marketplace vendor.',
};

export default function BecomeAVendorRegisterLayout({ children }) {
  return children;
}
